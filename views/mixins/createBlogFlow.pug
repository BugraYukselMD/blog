mixin createBlogFlow(blog)
    .container
        hr
    .container
        .container
            a(href=`/blog/${blog.urlExt}`).text-decoration-none.text-success 
                span 
                    strong #{blog.title}
        .container  
            .content
                p #{blog.subject}
        .container
            small.text-muted #{blog.date}
    