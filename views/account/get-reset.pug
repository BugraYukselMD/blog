extends ../layouts/main-layout.pug

block content   
    .container
        if errorMessage
            .container(style="width: 25rem;").mx-auto
                .alert.alert-danger !{errorMessage}
        .container(style="height: 1rem;")
        .card(style="width: 25rem;").mx-auto 
            .card-body
                form(action="/reset-password" method="POST")
                    input(type="hidden" name="_csrf" value=`${csrfToken}`)
                    .container(style="height: 1rem;")
                    .row
                        .col-xs-4.col-sm-4
                            label(for="email").col-form-label Mail Adresi
                        .col-xs-8.col-sm-8
                            input.form-control#email(type="text" name="email")
                    .container(style="height: 1rem;")        
                    .row
                        .col-xs-3.col-sm-3
                        .col-xs-6.col-sm-6
                            input(type="submit" value="Mail Gönder").btn.colorChangeable
                        .col-xs-3.col-sm-3